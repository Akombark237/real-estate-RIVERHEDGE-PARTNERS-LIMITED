# ========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ========================================
# Copy this file and configure for your production environment
# DO NOT commit the actual .env.production file to Git!

# Django Settings
SECRET_KEY=<GENERATE-A-NEW-SECRET-KEY>
DEBUG=False
ALLOWED_HOSTS=your-backend-url.onrender.com

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# CORS Settings (your frontend URL)
CORS_ALLOWED_ORIGINS=https://your-frontend-url.onrender.com,https://www.your-domain.com

# Email Settings (Gmail example)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=RIVERHEDGE PARTNERS <noreply@riverhedgepartners.com>

# Frontend URL (for email links)
FRONTEND_URL=https://your-frontend-url.onrender.com

# ========================================
# HOW TO GENERATE SECRET_KEY:
# ========================================
# Run this command in Python:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# ========================================
# HOW TO GET GMAIL APP PASSWORD:
# ========================================
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification
# 3. Go to App Passwords
# 4. Generate a new app password for "Mail"
# 5. Use that password in EMAIL_HOST_PASSWORD

# ========================================
# RENDER.COM DEPLOYMENT:
# ========================================
# Add these environment variables in Render Dashboard:
# - SECRET_KEY
# - DEBUG=False
# - ALLOWED_HOSTS=your-app.onrender.com
# - DATABASE_URL (automatically set by Render PostgreSQL)
# - CORS_ALLOWED_ORIGINS=https://your-frontend.onrender.com
# - EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# - EMAIL_HOST=smtp.gmail.com
# - EMAIL_PORT=587
# - EMAIL_USE_TLS=True
# - EMAIL_HOST_USER=your-email@gmail.com
# - EMAIL_HOST_PASSWORD=your-app-password
# - FRONTEND_URL=https://your-frontend.onrender.com

